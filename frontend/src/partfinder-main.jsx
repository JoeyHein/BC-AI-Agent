import React from 'react';
import ReactDOM from 'react-dom/client';
import PartFinder from './components/PartFinder/PartFinder';
import './index.css';

ReactDOM.createRoot(document.getElementById('partfinder-root')).render(
  <React.StrictMode>
    <PartFinder />
  </React.StrictMode>
);
