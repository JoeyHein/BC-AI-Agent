/**
 * Re-export the portal DoorPreview. The widget used to ship a forked copy
 * that drifted (maxWidth, SVG ids, commercial full-view types). One file
 * now renders both the dealer portal and this embed.
 */
export { default } from '../../frontend/src/components/DoorPreview.jsx'
